# cotp configuration (install.sh creates ~/.config/cotp/config.yaml with these defaults).
# Override: export COTP_CONFIG=/path/to/config.yaml
#
# vault_path: qr-vault.yaml for get; put merges here when set.
# qr_image_dir: default folder for put/read without -f.

vault_path: ~/.config/cotp/qr-vault.yaml
qr_image_dir: ~/Downloads/Screenshots
