# Optional cotp configuration.
# Install as: ~/.config/cotp/config.yaml
# Or set COTP_CONFIG to the path of this file.
#
# vault_path: where qr-vault.yaml lives (used by get, and by put when set).
# qr_image_dir: default folder for put/read without -f; base path for relative -f.

# vault_path: ~/secrets/qr-vault.yaml
# qr_image_dir: ~/Downloads/Screenshots
